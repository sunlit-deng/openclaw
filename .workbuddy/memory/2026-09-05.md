# 2026-09-05

- 【数据复盘】老大反馈"评论优化任务 1 小时未完成"。分析当天 11 个 preflight receipt：全部 cacheHit=false（迭代改码，内容指纹未变才该命中，属正常）；focused tests 本身很快（2-45s）；并行生效（pr-138819 valSum 8.9m vs wall 4.5m）。真瓶颈在 targeted check 内部：`tsgo:core:test` 分片冷启动 587s（issue-127809）/ 1200s（issue-138620，新 worktree 首跑）+ oxlint type-aware 2 文件 689s（疑似与 tsgo 并行抢 CPU 被饿）。
- 【根因 1】tsbuildinfo 按 worktree 存放（.artifacts/tsgo-cache/），新 worktree 每次付 10-20 分钟冷启动。实测 tsbuildinfo 源路径为相对路径（../../src/...）→ 跨 worktree 可移植。
- 【修复 1】新增 seed-openclaw-tsgo-cache.sh：从最新 sibling worktree 复制 tsbuildinfo 快照到新 worktree（tsgo 按 hash 复验、只重建过期文件，全过期则退化为全量，正确性安全）。已接入 new-openclaw-worktree.sh 和 prepare-openclaw-pr-worktree.mjs（git worktree add 之后）。实测：复制 17 个文件 + 幂等跳过 + 最新 sibling 启发式均正确。
- 【根因 2 + 修复】targeted planner bug：extensions surface 映射到不存在的 config/tsconfig/oxlint.extensions.json（OpenClaw 从未有此文件）→ extensions 类 PR 必挂 lint（pr-138819 实锤，白付 4.5m）。上游真身是 extensions/tsconfig.json（check-changed.mts:120），已改 targeted-validation.mjs:106 对齐。
- 回归：validate.sh 49/49。
- 【遗留观察】oxlint type-aware 冷启动重（689s/2 文件）、tsgo 分片 warm 墙上时间，均在上游 targeted check 内部，需上游改动；failureSummary/logPath 落盘在 pr-138819 已实战验证有效（诊断零重跑）。
- 【环境坑】沙箱 shell cd 不跨命令持久导致 MODULE_NOT_FOUND 假象（实际是 cwd 错了）；timeout 命令 macOS 无自带，用 Bash 工具的 timeout 参数。
