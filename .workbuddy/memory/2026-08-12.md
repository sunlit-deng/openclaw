# 2026-08-12 工作日志

## auto-pr-openclaw 技能可用性验证 + 软链接创建

- 验证 `auto-pr-openclaw` 技能在当前工作空间可完整使用：scripts(30个)/references(6份)/agents 齐全，gh 已认证(sunlit-deng)，git 身份匹配，workspace/openclaw 结构完整(repos/worktrees/outputs/accounts.json/.codegraph-cache 均在)。
- 发现本地 openclaw 仓库 main 落后 origin/main 703 提交，起 PR 前需先 fetch。
- 仿照 `.claude/skills/auto-pr-openclaw -> ../../.codex/skills/auto-pr-openclaw` 模式，在项目级 `.workbuddy/skills/` 下创建同名软链接指向同一源。这样 WorkBuddy 读项目级技能时可直接发现，无需复制文件。
- 路径：`/Volumes/Sunlit/auto-pr/.workbuddy/skills/auto-pr-openclaw` → `../../.codex/skills/auto-pr-openclaw`
- 注意：用户级 `~/.workbuddy/skills/auto-pr-openclaw/`(8/11更新) 比工作空间 `.codex/skills/auto-pr-openclaw/`(8/7更新) 更新，是独立副本非软链。脚本路径以 `.codex/` 为准（SKILL.md 命令均用该相对路径）。

## 删除用户级 auto-pr-openclaw 技能副本

- 老大要求删除 `~/.workbuddy/skills/auto-pr-openclaw/`（用户级副本，不要了）。
- 执行 trash 时系统报 file not found——该目录在执行前已不存在，最可能是上一轮建项目级软链接后技能系统同名去重自动清理了用户级副本（skills 目录下存在 migration 配置文件）。
- 最终状态：用户级 `~/.workbuddy/skills/` 仅剩 `ifind-finance-data` + 两个 migration json。技能正本唯一存在于工作空间 `.codex/skills/auto-pr-openclaw/`，项目级软链接 `.workbuddy/skills/auto-pr-openclaw` 正常工作，不受影响。

## 本地候选挖掘：两个新方向（scout 均 10 分 high）

- 背景：已有 10 个候选（media-notice 形态已做 Slack/Tlon/Telegram 三条，不可再用）。老大要求新方向。
- 从近期 merged PR 提取两个新形态：①"失败时保留旧产物"原子发布（#122568 memory-wiki / #122580 qa-lab / #122550 camera，核心 helper `src/infra/replace-file.ts` replaceFileAtomic，扩展经 `openclaw/plugin-sdk/security-runtime` 暴露）；② transient 网络错误共享分类器（#122535 引入 `src/infra/retryable-network-errors.ts` isTransientNetworkError，修 #91675）。
- 候选 A：`candidate-plan-doctor-transcript-atomic.json` — doctor-session-transcripts.ts 两处修复路径备份后直写活 transcript，写失败会截断用户数据且不恢复 .bak。查重清（120781/119156 不碰同文件）。scout 10/high。
- 候选 B：`candidate-plan-failover-undici-transient.json` — failover classification-rules.ts 的 TIMEOUT_ERROR_CODES 缺全部 UND_ERR_* Undici 码，结构化 code 信号分类为 null 不 failover；共享分类器已覆盖同批码。查重清（四层搜索零命中）。scout 10/high。
- 教训：scout 把 `policy.unresolvedChoices` 任何条目都当 early stop（"unresolved policy"），证据类 TODO 要写进 comparableProof.boundary 而不是 policy。
- 注意：repos/openclaw 的 fork remote 叫 RileyJJY，origin 是 openclaw/openclaw；/Volumes/Sunlit/auto-pr 本身也是 git repo（remote 名叫 openclaw），git 命令务必用 `git -C` 指定仓库。

## 候选A实施：doctor transcript 原子写入（local-doctor-transcript-atomic）

- Worktree `worktrees/local-doctor-transcript-atomic`，分支 `sunlit/fix/doctor-transcript-atomic`，base 79ac476b0f7，账号 sunlit (sunlit-deng)。local-candidate 无 issue 号，用 write-workflow-state.mjs --mode local-candidate 手写 workflow.json（脚本只支持 --issue 数字）。
- 修复：doctor-session-transcripts.ts 两处修复路径改走 `replaceFileAtomic`（preserveExistingMode:true, syncTempFile/syncParentDir:true, tempPrefix=basename），抽了 writeRepairedTranscript 公共函数。生产 +20/-6。
- 测试双 mock 注入：node:fs/promises writeFile（base 直写路径会命中→截断）+ ../infra/replace-file.js replaceFileAtomic（head 路径命中→抛错）。注意 fs-safe 是外部 npm 包被 vitest externalize，vi.mock node:fs 对它无效；replaceFileAtomic 的 temp 写无 test-hook（beforeSiblingTempWrite 是另一条流程的）。
- 证据：base 两新测试 fail（transcript 被截成 partial）/ head 16/16 pass；tsx 真实模块 driver head 跑通修复+备份+0640 权限保留+无 temp 残留；负控=健康 transcript 不动。
- 回执：duplicate-check live 0 重复（9 相关 PR）、candidate-score 83 promising、proof-receipt passed（kind=production-module-boundary）。
- 坑：Bash 工具每条命令独立 shell，cwd 不保留，vitest/runner 必须每条都 cd 或写临时脚本文件执行。openclaw CLI 需 node>=22.22.3（managed 22.22.2 不行），用 system node 24.18.0；CLI 还要全量 build，跳过 CLI 级证明。
- preflight 首跑 FAIL：测试 mock 里 `actual.default` 在 node:fs/promises 类型上不存在（TS2339）→ 改 `{ ...actual, writeFile }`。amend 后新 HEAD ad1488f48c2，所有回执 SHA 已刷新。
- preflight 二跑 43s 全 PASS（重检查指纹复用）。candidate-score 88/strong，A-readiness possible（仅缺 realCallChainProof）。gate-summary 0 blockers，已呈人工门等批准（HEAD ad1488f48c2 / body 63d7cd28）。
- **已发布**：老大批准后 publish-openclaw-pr.mjs 一次通过（新 PR 需额外 `--title` + `--head sunlit-deng:<branch>`）→ **PR #122628** https://github.com/openclaw/openclaw/pull/122628，head/body 哈希与批准值一致，maintainerCanModify=true。未发 re-review（等指示）。

## 复盘：候选A+114625 两单的慢因与优化清单（老大已看过）

- 慢因三层：①重型校验冷启动（preflight 5min、tsgo 跨 worktree 全局锁互等、node_modules 2GB/个）；②我的执行失误（cwd 不保留导致同命令重发 5 次、worktree 路径算错、测试缝选错两轮）；③技能文档缺口（local-candidate intake 没写、publisher --title/--head 发布时才报错、scout/proof-receipt schema 各返工一次）。
- 优化清单（按收益）：1) 技能补 local-candidate intake（--mode local-candidate --topic 或文档写手动法）；2) gate-summary 输出可直接复制的完整 publish 命令；3) preflight 前先跑 14s 增量 tsgo owning lane 抓类型错；4) 技能开头加环境卡（node 24.18.0 绝对路径、env -u NODE_OPTIONS、sandbox 豁免）；5) 我的纪律：长命令落 /tmp 脚本、git 一律 -C 绝对路径；6) 中期：heavy-check 锁按 lane/worktree 细化。
- 技能改进已提议给老大，等指示再动 auto-pr-openclaw（非 agent_created，不擅自改）。

## 六项优化已全部落地（老大批准"123456都改了"）

- #1 `new-openclaw-worktree.sh` 支持 `--mode local-candidate --topic`（worktree local-<topic>、branch sunlit/fix/<topic>、workflow mode=local-candidate 无 issue），参数校验路径已测。
- #2 `openclaw-gate-summary.mjs` 新增 "Publish command" 段：可直接复制的完整 publish 命令，新 PR 自动带 `--title`（取 head commit subject）和 `--head <login>:<branch>`，已发布 PR 则省略。两条路径都实测过。
- #3 SKILL.md "Executable local checks" 加 preflight 前先单跑属主 tsgo lane（dry-run 预览 → 直接跑 → 暖缓存进 preflight）。
- #4/#5 SKILL.md 新增 "Machine Environment and Shell Hygiene"：node 24.18.0 绝对路径、`env -u NODE_OPTIONS` 症状→解法、cwd 不保留/git -C/多步序列写 /tmp 脚本。
- #6 `openclaw-preflight.mjs` 顶部 `OPENCLAW_HEAVY_CHECK_LOCK_SCOPE ??= "worktree"`（上游本来就支持，锁从共享 .git 切到 per-worktree .artifacts）；并从 heavy 指纹的 relevantEnvironment 里排除该变量（锁作用域不影响校验结果，不该 invalidate 缓存）。
- 注意：preflightScriptSha256 在指纹里，本次改了 preflight.mjs → 下一次 preflight 必冷跑一次，之后回暖。
- 另一会话的未提交改动（rebase-only SHA 刷新相关 4 文件）未触碰。

## PR #114625 按 ClawSweeper 评论优化并发布

- 任务：根据 ClawSweeper 评论优化 PR 114625（nextcloud-talk room-kind lookup retry）。
- 三件事：①P3 finding 修复——删除 inbound.ts/webhook-spool.ts 里重复的 `ChannelIngressMonitorDeliveryResult` union 和从不抛出的 `NextcloudTalkRetryableWebhookError`，改从 `openclaw/plugin-sdk/channel-outbound` 导入共享类型直接返回（5 文件 −27 行）；②在最新 head 重跑 proof-live.ts 实时证明并更新 PR body（base fea6d96378cb 无重试 vs head 有重试）；③CI 失败定性为 main 预存问题：check-test-types 失败在 extensions/browser/cdp.helpers.test.ts（PR 未触碰），根因 main #122614 引入 DNS lookup 类型错误，修复 PR #122599 未合并，rebase 无法解决。
- 环境坑（重要）：本机 managed Node 22.22.2 内嵌 SQLite 3.51.2 不满足 OpenClaw 要求（需 >=22.22.3），vitest/tsgo 会失败——必须用 `/Users/yangjiajun/.nvm/versions/node/v24.18.0/bin/node`（24.18.0）；WorkBuddy 通过 NODE_OPTIONS 注入 safe-delete shim，会拦截 tsgo 清理 `.git/openclaw-local-checks/heavy-check.lock`（count 812 > 阈值 50），跑 preflight/tsgo 需 `env -u NODE_OPTIONS`。两条都需 dangerouslyDisableSandbox。
- preflight targeted profile 全绿（format+oxlint+tsgo extensions+42 focused tests），gate 0 blockers，maintainerCanModify true。
- 发布：publish-openclaw-pr.mjs 需 `--approved-head <sha> --approved-body-sha <sha256> --push-remote sunlit-deng`（参数名是 --approved-body-sha 不是 --approved-body-sha256）。发布后 head=202f78c9cc9d，body 含 tested-head: 202f78c9cc9d，maintainerCanModify true。
- 未发 @clawsweeper re-review（等老大单独指示）。
- PR #110429（comfy workflowPath 文件大小上限，账号 jjy/RileyJJY）：按老大要求先 rebase 再按 ClawSweeper 评论优化并发布。
  - 连续两次 rebase：先到 4fcd9e12（conflict-rebase 解决 baseline 生成文件冲突，重新生成 config-baseline.*），preflight 又发现 main 前进到 c5ba4efb（Peter Steinberger #122636 删除 media test globals），与 image-generation-provider.test.ts 真实冲突 → 再 rebase 到 c5ba4efb，适配新结构（删除 test-support.ts 的 setComfyFetchGuardForTesting 全局，改用 vi.mock + ssrfGuardState.actual；删 5 处多余 setComfyFetchGuardForTesting 调用）。
  - ClawSweeper P2 修复：重新生成 baseline 后与最新 main 完全一致（core 2300/channel 3582 相同），仅 plugin 3997→3998（本 PR 新增配置项），满足"只含 plugin-only 变化"要求。comfy 测试 37→38 passed。
  - 坑 1：worktree 默认 git identity 是 sunlit-deng，rebase 后 committer 错 → 需先 `git config user.name/email` 改成 RileyJJY <0668000974@xydigit.com> 再 rebase。
  - 坑 2：rebase --continue 会打开编辑器导致 exit 137 挂起，用 `GIT_EDITOR=true git rebase --continue`。
  - 坑 3：publish-openclaw-pr.mjs 用普通 push（无 force），rebase 后 non-fast-forward 失败；需先用 jjy token 手动 `git push --force-with-lease=refs/heads/<ref>:<旧head> https://<login>:<token>@github.com/RileyJJY/openclaw.git`（先 fetch 验证远程 head 未变）再重跑 publisher。force push 脚本在 /tmp/push-110429-force.sh（勿删，可复用）。
  - 最终：head=58822c439d，body sha256=60d66eb2，maintainerCanModify true，mergeable MERGEABLE。未发 re-review。

## PR #122628 按 ClawSweeper 评论补真实 proof（waiting human gate）

- 任务：根据 ClawSweeper 评论优化 PR 122628（doctor transcript 原子写入），补充真实 proof。核心 finding [P2]：原测试 mock `replaceFileAtomic` 直接 throw，绕过了真实 helper 的 staged-write 路径。
- 修复：`src/commands/doctor-session-transcripts.test.ts` 删除直接 throw 的 mock 和 `node:fs/promises` writeFile mock，`replaceFileAtomic` 改为 `vi.fn(actual.replaceFileAtomic)`（spy 真实函数），两个失败测试用 sibling memory-wiki 模式：`mockImplementationOnce` 调真实 helper + `beforeRename` hook 写 partial bytes 后抛 ENOSPC，断言字节不变 / mode 0640 / 无 .tmp 残留。head 16/16 通过，base 79ac476b0f7 上 2 fail/14 pass（对比有效）。
- proof-failure.ts：真实 `replaceFileAtomic`（同一 production 原语）+ beforeRename 注入失败 → BYTES IDENTICAL / MODE PRESERVED 640 / TEMP LEFTOVERS [] / original transcript intact。脚本存 outputs/pr-122628/。
- PR body 更新：新增失败路径 proof、base/head 对比、head SHA 改为 34c418e538e（新 commit）。
- 环境坑（本次特有）：prepare-openclaw-pr-worktree.mjs 首跑因 managed Node 22.22.2 不满足 preinstall 挂掉（worktree 已建但 workflow.json 未写）；用 system node 24.18.0 重跑报 worktree 已存在 → 手动 `write-workflow-state.mjs` 补写 workflow.json。依赖首次安装用了相对 root 导致 store 落在 worktree 内部（node_modules/.modules.yaml storeDir 错），preflight FAIL "node_modules uses shared store" → 删 node_modules + 错误 store 重装（`env -u NODE_OPTIONS CI=true` + 绝对路径）后 PASS。
- preflight targeted 全 PASS（lint+types+focused tests，heavy 缓存复用），gate-summary 0 blockers，maintainerCanModify true，body sha256=686bb033。**已呈人工门等批准，未发布、未 re-review。**
