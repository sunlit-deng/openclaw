# 2026-09-03

- 分析 openclaw-preflight.mjs 耗时问题，输出优化方案：spawnSync 全串行 → 改异步并行（fetch ∥ 本地 git 读、check lane ∥ test lane）、git fetch 加 TTL 缓存、批量 rev-parse、跳过 pnpm --version 每次冷启动、preflightScriptSha256 导致脚本改动即全缓存失效的问题。建议先按 docs/performance.md 计划加 per-phase timing（networkMs/gitMs/validationMs）再动手。
- 【已实施】preflight 优化第一轮落地：fetch 改 worker_threads + spawnSync 后台线程跑（关键坑：主线程 Atomics.wait 等 async spawn 的 exit 回调会死锁——事件循环被阻塞，回调永远不触发；正确做法是 worker 线程执行 spawnSync + SAB 标志位）；fetch TTL 用 OPENCLAW_PREFLIGHT_FETCH_TTL_MS（默认 0 关闭，开了会破坏 preflight-pinned-base 测试"push 后立刻重跑必须看到新 main"的断言）；3 次 rev-parse 合批、branch 并入 status --branch（detached HEAD 有 fallback）、pnpm --version/store path 24h 缓存到 outputs/.preflight-tool-cache.json；receipt 新增 timing 字段（networkMs/gitMs/validationMs/otherMs/processSpawns/totalMs）。validate.sh 44/44 通过。
- 【环境坑】沙箱 shell PATH 缺 /opt/homebrew/bin，pnpm 找不到会导致测试假失败（shared pnpm store ENOENT）；跑测试需 PATH="/opt/homebrew/bin:$PATH"。
- 【已实施】preflight 优化第二轮：heavy lane 并行（check ∥ focused tests）。startAsyncRun 的 worker 从 spawnSync 升级为异步 spawn（stdout/stderr 合计 1MB 截断上限，detached 进程组），新增 abort()——主线程 postMessage，worker 对 child 发进程组 SIGTERM（POSIX），wait() 超时先 abort 再 terminate。heavy 段重构：两条 lane 都无缓存命中时测试 lane 先行启动（worker），check lane 照旧主线程同步跑，join 后若 check 失败则 abort 测试 lane 并记原语义的 skippedCheck。fixture 验证：两 lane 时间戳完全重叠、check 秒败时 test 被杀在睡眠中（有 start 无 end stamp）、无孤儿进程。validate.sh 44/44。遗留：并行后 validationMs 是各 lane 耗时之和会大于墙上时间，docs/performance.md 已把 wallValidationMs 列为下一步。
- 【已实施】preflight 优化第三轮：失败诊断零重跑。根因：receipt 对 heavy check 用 includeOutput:false，失败输出完全丢失 → triage 只能全量重跑测试拿日志。改法：run() 加 captureFullOutput 选项（未截断 fullOutput）、worker 保留 ≤1MB 完整捕获；中央 persistFailedCheckArtifacts 在 receipt 写盘前对失败 check 落盘 outputs/<issue>/<slug>.failed.log（每 lane 一个文件、覆盖写），receipt 记 logPath + failureSummary（≤20 条失败标记行 + ≤20 个涉及文件）+ repro（command/cwd）。fixture 三场景验证（并行重叠、check 失败 abort、test 失败落盘 60081 字节 vs receipt 4KB）。validate.sh 44/44。遗留：上游 only-failures 重放（已列入 docs/performance.md 优先级）。
- 【已实施】preflight 优化第四轮：receipt timing 增加 wallNetworkMs/wallGitMs/wallValidationMs/wallOtherMs——每 category 用深度计数器跟踪活跃区间并集（首个 enter 记起点、最后一个 exit 结算，finalizePhaseWall 兜底未闭合区间），解决并行后 validationMs（各 lane 之和）虚高问题。fixture 实测：validationMs 3697 vs wallValidationMs 2107，差值恰为两 lane 重叠的 ~1.5s。validate.sh 44/44。

