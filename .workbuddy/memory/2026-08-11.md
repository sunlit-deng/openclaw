# 2026-08-11

## 本地候选筛选（auto-pr-openclaw）

基于 `origin/main = 29fd6aaf`（已 fetch 刷新）复核 8 个已有 candidate-plan。

**放弃 3 个**:
- 120758（QR small terminal）— 已被 #120827 合并修复
- 120671（memory_search deadline）— #121665 覆盖同区域 startup reconciliation
- 120408（config.patch ack）— early stop: unresolved policy

**降级 2 个**:
- 120735（Telegram sticker）— bot-message-context.body.ts 被大幅重写，新增 sticker vision 系统，候选前提过时
- 10354（message-tool channel schema）— 模块被 #121901 拆分，bare string 问题仍在 `message-tool-schema.ts:22` 但区域 unstable

**推荐**:
- TOP 1: 120561（agent.wait retryable finalization, high/strong）— 文件被 #121241 迁到 `src/gateway/agent-turn/agent-job.ts`，逻辑仍在，需更新 plan 路径
- TOP 2: 99277（Mattermost ack-reaction, high/review）— 最干净，shouldAckReaction 仍缺失，契约仍在

结果文件: `outputs/local-candidate-shortlist-20260811.md`

## 从代码出发挖新本地候选（formatInboundMediaUnavailableText sibling gap）

老大纠正：要的是从本地代码出发挖新候选，不是复核旧 plan。

从 `formatInboundMediaUnavailableText`（`src/channels/inbound-event/media.ts:85`）的 sibling gap 出发：
- 11 个 channel 已用此 helper（discord/feishu/googlechat/imessage/line/mattermost/msteams/signal/sms/whatsapp/zalo）
- matrix 有自己的等价实现，不是 gap
- **slack**: `media.ts:471` forwarded image 下载失败 `catch {}` 静默跳过，无 notice
- **tlon**: `index.ts:340` 下载失败只 log，`buildTlonMediaPrompt` 在 media.length==0 时只返回原文

两个候选都 score 10 / high / strong，无重复 PR。canonical precedent = signal `event-handler.ts:1208`。

plan: `candidate-plan-slack-media-notice.json` / `candidate-plan-tlon-media-notice.json`
scout: `candidate-scout-slack-media-notice.json` / `candidate-scout-tlon-media-notice.json`
shortlist: `outputs/local-candidate-shortlist-20260811-fromcode.md`
