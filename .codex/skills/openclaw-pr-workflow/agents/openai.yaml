interface:
  display_name: "OpenClaw PR Workflow"
  short_description: "Prepare OpenClaw PRs with review gates."
  default_prompt: "Use $openclaw-pr-workflow to handle an OpenClaw issue or PR from triage through pre-push review."
